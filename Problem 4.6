# include <stdio.h>

int main()
{
    double x,y;
    
    printf("输入x的值\n");
    scanf("%lf",&x);
    
    if(x<1)
    {
        y=x;
    }
    else if(1<=x && x<10)
    {
        y=2*x-1;
    }
    else
    {
        y=3*x-11;
    }
    printf("y的值是%lf\n",y);
    
    return 0;
}
